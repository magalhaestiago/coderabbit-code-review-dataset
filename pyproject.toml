[project]
name = "coderabbit-code-review-dataset"
version = "0.1.0"
description = "Dataset collection and analysis scripts for CodeRabbit GitHub activity."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib",
    "pandas",
    "pyarrow",
    "python-dotenv",
    "requests",
]

[dependency-groups]
dev = [
    "ipykernel",
    "notebook",
]
